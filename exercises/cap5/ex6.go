// Crie um programa que utilize a declaração switch, sem switch statement especificado

package main

import "fmt"

func main() {
	x := 1
	switch {
	case x == 1:
		fmt.Printf("Uou, X é %v", x)
	case x == 2:
		fmt.Printf("Uou, X é %v", x)
	case x == 3:
		fmt.Printf("Uou, X é %v", x)
	}
}
